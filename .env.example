# Frontend Environment Variables
# Note: The GlobalGiving API key is now stored securely in Cloudflare Functions
# See .dev.vars.example for server-side configuration
# Create a .env file if you need frontend-specific environment variables
