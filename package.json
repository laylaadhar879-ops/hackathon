{
  "name": "hackathon",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev --port 8788 -- vite",
    "dev:vite-only": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:functions": "wrangler pages dev dist"
  },
  "devDependencies": {
    "vite": "^7.2.4",
    "wrangler": "^4.56.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "zod": "^4.2.1"
  }
}
